name: Markdown Lint

on:
  pull_request:
    paths:
      - "**/*.md"

jobs:
  markdown-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Markdown Linter
        uses: avto-dev/markdown-lint@v1
